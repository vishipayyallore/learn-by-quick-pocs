# Simple NodeSJ TypeScript API for Google Recaptcha v3 POC

This is a simple NodeJS API that uses TypeScript and Express to validate a Google Recaptcha v3 token.

## Commands

```powershell
npm install typescript ts-node express @types/express morgan @types/morgan axios @types/axios nodemon

tsc --init

npm install dotenv --save
```
